name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      RELEASE_ZIP: Thoughtline-${{ github.ref_name }}-chrome-unpacked.zip
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @thoughtline/extension exec playwright install --with-deps chromium
      - run: pnpm test:e2e
      - run: pnpm release:zip
      - name: Prepare unpacked Chrome archive
        run: |
          mkdir -p release
          source_zip="$(find apps/extension/.output -maxdepth 1 -type f -name '*-chrome.zip' -print -quit)"
          test -n "${source_zip}"
          cp "${source_zip}" "release/${RELEASE_ZIP}"
      - name: Validate unpacked package layout
        run: unzip -Z1 "release/${RELEASE_ZIP}" | grep -Fxq manifest.json
      - name: Create checksums
        run: sha256sum "${RELEASE_ZIP}" > SHA256SUMS.txt
        working-directory: release
      - uses: softprops/action-gh-release@v2
        with:
          name: Thoughtline ${{ github.ref_name }}
          body_path: .github/RELEASE_NOTES.md
          fail_on_unmatched_files: true
          files: |
            release/${{ env.RELEASE_ZIP }}
            release/SHA256SUMS.txt
