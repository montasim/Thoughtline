# Bound AI-assisted layout calibration evidence

AI-Assisted Layout Calibration may send raw LinkedIn DOM values to the selected free OpenRouter model and, under Cross-Provider Fallback, Gemini then Groq because provider reasoning can recover layouts that deterministic rules cannot. The request is limited to explicit user-approved Calibration Evidence around the selected target rather than the complete page; the returned recipe remains local and must pass deterministic multi-example validation and user confirmation before persistence.
